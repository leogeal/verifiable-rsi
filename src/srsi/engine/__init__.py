"""Domain-agnostic self-improvement engine: genome, schema, mutator, gates,
sandbox, lineage, and the recursive improver loop. Import submodules directly
(e.g. ``from srsi.engine.improver import RecursiveImprover``)."""
